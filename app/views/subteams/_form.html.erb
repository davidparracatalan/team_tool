<%= form_for(@subteam) do |f| %>
  <% if @subteam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subteam.errors.count, "error") %> prohibited this subteam from being saved:</h2>

      <ul>
      <% @subteam.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.label :start_date %>
    <%= f.date_select :start_date %>
    <%= f.label :foreseen_end_date %>
    <%= f.date_select :foreseen_end_date %>
    <%= f.label :end_date %>
    <%= f.date_select :end_date %>
  </div>
  <%= f.submit "Create", class: "btn btn-large" %>
  <%= link_to 'Go back home', root_path, class: "btn btn-large"%>
<% end %>