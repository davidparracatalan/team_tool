<div class="page-header">
  <h1>Editing <%=@teammate.name%>'s data</h1>
</div>
<%= form_for @teammate do |f| %>
<%= render 'fields', f: f %>
<div>
  <%= f.submit "Update", class: "btn" %>
</div>
<% end %>